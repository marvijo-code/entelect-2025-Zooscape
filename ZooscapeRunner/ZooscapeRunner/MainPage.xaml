<Page
    x:Class="ZooscapeRunner.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ZooscapeRunner"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:ZooscapeRunner.ViewModels"
    mc:Ignorable="d">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Title  -->
        <TextBlock
            Grid.Row="0"
            HorizontalAlignment="Center"
            FontSize="32"
            FontWeight="Bold"
            Margin="0,0,0,20"
            Text="Zooscape Runner" />

        <!--  Controls  -->
        <StackPanel
            Grid.Row="1"
            Margin="0,0,0,20"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <Button x:Name="StartAllButton" Content="Start All" Margin="5" Command="{Binding StartAllCommand}" />
            <Button x:Name="StopAllButton" Content="Stop All" Margin="5" Command="{Binding StopAllCommand}" />
            <Button x:Name="RestartAllButton" Content="Restart All" Margin="5" Command="{Binding RestartAllCommand}" />
            <TextBlock
                Margin="20,0,0,0"
                VerticalAlignment="Center"
                Text="{Binding AutoRestartText}" />
        </StackPanel>

        <!--  Process List  -->
        <ListView Grid.Row="2" x:Name="ProcessListView" ItemsSource="{Binding Processes}">
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid Padding="10,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="120" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column="0"
                            FontWeight="SemiBold"
                            Text="Process" />
                        <TextBlock
                            Grid.Column="1"
                            FontWeight="SemiBold"
                            Text="Status" />
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="viewmodels:ProcessViewModel">
                    <Grid Padding="10,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="120" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Name}" />
                        <TextBlock Grid.Column="1" Text="{Binding Status}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
